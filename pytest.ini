[pytest]
testpaths = tests
# integration テストはモデルが必要なため通常実行から除外
# 実行するには: pytest -m integration
addopts = -m "not integration"
markers =
    integration: requires actual GGUF model (deselect with '-m "not integration"')
